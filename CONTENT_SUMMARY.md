# Index of Files in 'oaustegard/bookmarklets/'

## [.DS_Store](https://github.com/oaustegard/bookmarklets/blob/main/.DS_Store)

The provided files are binary files and cannot be read as code. If you have any specific questions or need assistance with a particular file, please let me know.

## [.gitignore](https://github.com/oaustegard/bookmarklets/blob/main/.gitignore)

This code file contains a comprehensive list of file patterns to be ignored by version control systems like Git when working with Visual Studio projects. It includes user-specific files, build results, auto-generated files, cache directories, test results, code coverage results, and various tool-specific files generated by Visual Studio and its add-ons.

## [LICENSE](https://github.com/oaustegard/bookmarklets/blob/main/LICENSE)

This is the MIT License, a permissive open-source license that allows users to freely use, modify, and distribute the software as long as the original copyright notice and license terms are included. The license disclaims any warranty and limits liability for damages.

## [README.md](https://github.com/oaustegard/bookmarklets/blob/main/README.md)

It seems like the user has a collection of bookmarklets for various purposes, with each file serving a different function.

## [StravaDisplayRideSummaries.js](https://github.com/oaustegard/bookmarklets/blob/main/StravaDisplayRideSummaries.js)

This JavaScript code retrieves stored ride summaries from local storage, removes specific phrases from the summaries, and displays the formatted data in a new window.

## [StravaExcerpt.js](https://github.com/oaustegard/bookmarklets/blob/main/StravaExcerpt.js)

This code file creates a bookmarklet that extracts and displays highlights and interesting segments from a webpage related to cycling activities. It defines functions to fetch highlights, parse segments, filter interesting segments based on thresholds, combine highlights, format segments as a bulleted list, and display the ride summary in a new window. The output includes rider information, ride details, top achievements, and details of interesting segments meeting specified power and speed thresholds.

## [StravaShortExcerpt.js](https://github.com/oaustegard/bookmarklets/blob/main/StravaShortExcerpt.js)

This JavaScript code retrieves the text content of the section element with the id "heading" and stores it in the local storage. It also retrieves an array of ride summaries from the local storage, adds the new summary to the array, and then stores the updated array back in the local storage.

## [TwitterConversationExtractor.js](https://github.com/oaustegard/bookmarklets/blob/main/TwitterConversationExtractor.js)

This JavaScript code collects tweets and their popularity data from a webpage by scrolling through the page and extracting relevant information from each tweet article. It then opens a new window displaying the collected data in a formatted manner.

## [_c.js](https://github.com/oaustegard/bookmarklets/blob/main/_c.js)

This code snippet creates a bookmarklet that ensures the console object is available and aliases it as _c for concise usage. It appends a script element to the document body that assigns the console object to _c if available, and copies all its properties to _c.

## [_copyToClip.js](https://github.com/oaustegard/bookmarklets/blob/main/_copyToClip.js)

This code defines a utility function `copyToClip` that copies text to the clipboard. It uses the `execCommand` method to copy both formatted HTML and plaintext. The function adds an event listener for the "copy" event, sets the clipboard data with the provided HTML and plaintext, prevents the default action, executes the copy command, and then removes the event listener.

## [_lg.js](https://github.com/oaustegard/bookmarklets/blob/main/_lg.js)

This code snippet creates a utility function `_lg` that can be used for logging messages to the console in bookmarklets where `console.log` does not work. It appends a script element to the document body that defines the `_lg` function to log messages to the console.

## [_turndown_min.js](https://github.com/oaustegard/bookmarklets/blob/main/_turndown_min.js)

This code file is a minified version of Turndown.js (v7.12) obtained from a specific URL. It contains a TurndownService class that provides functionality for converting HTML to Markdown. The code includes various rules for converting different HTML elements to Markdown syntax. It also handles escaping characters and provides methods for converting HTML content to Markdown.

## [arxiv_abstract.js](https://github.com/oaustegard/bookmarklets/blob/main/arxiv_abstract.js)

This JavaScript code is a bookmarklet that checks if the current webpage is on arxiv.org. If it is, it modifies the URL to switch from a PDF view to an abstract view and then opens the new URL in a new tab. If the webpage is not on arxiv.org, it displays an alert message.

## [closest_anchor.js](https://github.com/oaustegard/bookmarklets/blob/main/closest_anchor.js)

This JavaScript code snippet retrieves the parent element of the selected text in the current webpage, then finds the closest ancestor element with an ID attribute or a name attribute if it is an anchor element. Finally, it updates the URL hash with the ID or name of the target element.

## [confluence_link.js](https://github.com/oaustegard/bookmarklets/blob/main/confluence_link.js)

This JavaScript code snippet creates a script that extracts metadata from a Confluence page (such as page title and space name), constructs a markdown link and an HTML link, and then copies the links to the clipboard. If the necessary metadata is not found, it displays an alert. The script also logs the copied link to the console and handles errors by logging them and displaying an alert message.

## [confluence_preview_attachments.js](https://github.com/oaustegard/bookmarklets/blob/main/confluence_preview_attachments.js)

This JavaScript code modifies a table on a webpage by adding a 'Preview' column to the header row and inserting preview images in each row based on the file link provided in the 'filename' column. The preview images are displayed with a maximum width of 150px.

## [copy_as_markdown.js](https://github.com/oaustegard/bookmarklets/blob/main/copy_as_markdown.js)

This JavaScript code dynamically creates a TurndownService object to convert selected HTML content into Markdown format. It also includes functions to get the selected HTML content, convert it to Markdown, copy it to the clipboard, and display the Markdown content in a new window. The code logs messages at different stages of the process.

## [copy_url.js](https://github.com/oaustegard/bookmarklets/blob/main/copy_url.js)

This JavaScript code creates a pop-up window with a form to input a URL and link title. It then generates a Markdown formatted link based on the input and copies it to the clipboard when the "Copy & Close" button is clicked. The pop-up window closes after copying the link.

## [css_colors.js](https://github.com/oaustegard/bookmarklets/blob/main/css_colors.js)

This JavaScript code defines a function `rgbToHsl` to convert RGB color values to HSL format. Another function `extractColorRules` extracts color rules from the stylesheets of a webpage, sorts them based on their HSL values, and displays color swatches with corresponding CSS rules in a new window. The code then calls `extractColorRules` to execute this functionality on the current webpage.

## [drag_drop_and_delete.js](https://github.com/oaustegard/bookmarklets/blob/main/drag_drop_and_delete.js)

This JavaScript code defines a functionality where elements on a webpage can be outlined in blue when hovered over, moved around by clicking and dragging, and deleted by pressing the "Delete" or "Backspace" keys. The code also allows for cleanup by pressing the "Escape" key, which removes the outline and event listeners.

## [edge.js](https://github.com/oaustegard/bookmarklets/blob/main/edge.js)

This JavaScript code snippet redirects the current webpage to be opened in the Microsoft Edge browser.

## [edit_page.js](https://github.com/oaustegard/bookmarklets/blob/main/edit_page.js)

This JavaScript code snippet toggles the `contentEditable` property of the `document.body` element between 'true' and 'false'. It also sets the `designMode` property of the document to 'on' when `contentEditable` is 'true', and 'off' when `contentEditable` is 'false'.

## [find_and_replace.js](https://github.com/oaustegard/bookmarklets/blob/main/find_and_replace.js)

This JavaScript code defines a function `replaceTextInHtml` that replaces text in the HTML content of a webpage. It prompts the user to enter text to find and text to replace, then calls the `replaceTextInHtml` function with the provided inputs. The function recursively traverses through the HTML elements to find and replace the specified text.

## [generate_md_files.py](https://github.com/oaustegard/bookmarklets/blob/main/generate_md_files.py)

This Python script utilizes the OpenAI API to generate Markdown documentation for JavaScript bookmarklet files in a specified directory. 

1. It defines functions to list files in a directory, read a file's content, invoke the API to generate documentation based on the content, and write the generated documentation to a Markdown file.

2. The script uses argparse for command-line argument parsing, allowing the user to specify the target directory and provide a list of file names to ignore.

3. It iterates through each file

## [jira_link.js](https://github.com/oaustegard/bookmarklets/blob/main/jira_link.js)

This JavaScript code defines a function `copyToClip` that copies HTML and plain text to the clipboard when triggered by a copy event. It then extracts various data from specific elements on a webpage (such as key, summary, status, priority, type, assignee) and constructs a markdown link and HTML link using this data. Finally, it combines the extracted data into a markdown format and an HTML format, and copies them to the clipboard using the `copyToClip` function.

## [markdown_body.js](https://github.com/oaustegard/bookmarklets/blob/main/markdown_body.js)

This JavaScript code defines a function that converts HTML content into Markdown format and copies it to the clipboard. It includes functions to identify the main content element on a webpage, remove certain elements from the content, and copy the Markdown content to the clipboard. The Markdown content is then displayed in a new browser window.

## [mastodon.social_profile.js](https://github.com/oaustegard/bookmarklets/blob/main/mastodon.social_profile.js)

This JavaScript code is a bookmarklet that retrieves the username from a meta tag with property "profile:username" on a webpage. If the username is found, it opens a new window with a URL specific to the Mastodon social network using the retrieved username. If the meta tag is not found or does not contain content, an alert is shown indicating that the bookmarklet only works on Fediverse sites with a proper username meta tag.

## [search_site.js](https://github.com/oaustegard/bookmarklets/blob/main/search_site.js)

This JavaScript code opens a new browser window with a Google search for the site specified by the current URL (excluding "www.") along with the search terms entered by the user.

## [speed_up_video.js](https://github.com/oaustegard/bookmarklets/blob/main/speed_up_video.js)

This JavaScript code dynamically increases the playback rate of videos found within iframes on a webpage. It first creates a script element to extend the console object with additional functionality. Then, it defines a function to recursively search for videos within iframes and increase their playback rate by 1.5. Finally, it calls the function to start the process on the current document.

## [strava_excerpt.js](https://github.com/oaustegard/bookmarklets/blob/main/strava_excerpt.js)

This code file creates a bookmarklet that extracts and displays highlights and interesting segments from a webpage related to a cycling activity on Strava. It defines functions to fetch highlights, parse segments, filter interesting segments based on thresholds, combine highlights, format segments as a bulleted list, and display a summary in a new window. The output includes rider information, ride details, top achievements, and details of interesting segments meeting specified power and speed thresholds.

## [strava_gpx_link.js](https://github.com/oaustegard/bookmarklets/blob/main/strava_gpx_link.js)

This JavaScript code snippet adds a "GPX" link next to elements with the attribute `data-field-name` set to "name" on a webpage. When clicked, the link will navigate to the GPX data export endpoint by appending "/export_gpx" to the original element's href.

